import React from 'react';
import {Icon,modelName,modalityName} from './design';
type Obj=Record<string,any>;
export function ModelGallery({models,selected,choose}:{models:Obj[],selected:string,choose:(m:Obj)=>void}){
 return <section className="model-gallery"><div className="section-heading"><div><p className="eyebrow">THE RIGHT MODEL FOR YOUR SONAR</p><h2>Choose your inspection specialist</h2><p>Different sensors, different targets. Each model keeps its own evidence and limitations.</p></div><span className="subtle-chip">EXPLICIT SENSOR MATCHING</span></div><div className="model-gallery-grid">{models.map((m,i)=><article className={'specialist-card '+(selected===m.id?'chosen':'')} key={m.id}><div className="specialist-top"><span className="specialist-symbol"><Icon name={i%2?'wave':'scan'} size={24}/></span><span>{m.modalities.map(modalityName).join(' / ')}</span></div><h3>{m.name||modelName(m.id)}</h3><div className="class-tags">{Object.values(m.classes||{}).map((c:any)=><span key={c}>{c}</span>)}</div><p>{m.evidence}</p><div className="specialist-bottom"><span>{m.download_mb?`${m.download_mb} MB · `:''}{m.state==='ready'?'Available':'Unavailable'}</span><button disabled={m.state!=='ready'} className={selected===m.id?'primary':''} onClick={()=>choose(m)}>{selected===m.id?'Selected':'Use model'}<Icon name="arrow" size={15}/></button></div>{m.source&&<a className="source-link" href={m.source} target="_blank" rel="noreferrer">Source & model licence ↗</a>}</article>)}</div></section>
}
