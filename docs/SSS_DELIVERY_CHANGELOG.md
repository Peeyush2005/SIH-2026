Version0.1.2: added boxes CLI exposing XTF start/chunk controls; aggregate cancellation/error receipts; numbered labels/context links; explicit candidate metadata and source-NMS provenance; conservative striping observation. Detector preprocessing, thresholds, weights, box mapping, source suppression and evaluation matching unchanged. Recognition improvement: none claimed. Existing failed acoustic verifier stays disabled. No new training.
